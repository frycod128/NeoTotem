# 永生图腾：需求、语义与验证基线

本文档是本项目的行为契约。实现以 Minecraft 26.2 与 NeoForge 26.2.0.66 的本地 patched sources 为准。

## 1. 内容物

- 模组只注册一个物品：`immortalitytotem:immortality_totem`（永生图腾）。
- 物品最大堆叠数为 1，稀有度为史诗。
- 物品携带原版 `DataComponents.DEATH_PROTECTION`，值为
  `DeathProtection.TOTEM_OF_UNDYING`。
- 配方固定为 3×3 九个 `minecraft:totem_of_undying`，产出一个永生图腾。
- 客户端模型以 `minecraft:item/totem_of_undying` 为静态 `layer0`，再叠加本模组的动画遮罩 `layer1`。

### 主手长按传送契约

- 玩家在主手持有图腾并按住右键时开始引导，引导时长为 60 tick，即 3 秒。
- 引导期间玩家获得满额击退抗性和 -20% 移动速度，并每 10 tick 在玩家位置播放一次
  `minecraft:block.amethyst_block.chime`，形成持续叮铃声。
- 引导开始时立即向玩家 `FoodData` 增加 20 点 Exhaustion，并施加 6 秒（120 tick）虚弱 V；
  重复开始引导时刷新虚弱剩余时间。
- 完整长按后，先在引导结束时的原地位置生成一次 `minecraft:flash` 粒子，再传送玩家到当前出生点。
- 出生点使用玩家当前重生配置（床/重生锚/`/spawnpoint`），不消耗重生锚；重生配置无效时回退到世界出生点。
- 只有 `ServerPlayer#teleport` 确认成功（返回非空）后，图腾才会进入 180 tick 物品冷却；
  传送被取消时不会进入冷却。冷却期间三个右键入口直接放行，只禁止再次开始引导，
  不影响图腾的持有、免死等其他功能。
- 中途松开右键、切换主手物品、物品被移除或玩家登出都会清理击退/速度修饰符并取消传送；
  这些中断与正常完成一样，都会叠加 6 秒（120 tick）失明 I，重复触发时累加 3 秒剩余时长。
- 开始时消耗度和虚弱效果不会因为引导中断被撤回。
- 引导倒计时由物品使用生命周期驱动；服务端仅保留一个表示“是否仍在引导”的状态，
  用于避免完成和中断重复结算失明效果。

## 2. “位于玩家物品栏”的精确定义

每次需要判定时都实时扫描 `Player#getInventory()`，不缓存结果。
Minecraft 26.2 的 `Inventory` 迭代器覆盖：

- 36 个快捷栏和主背包槽位；
- 四个护甲槽；
- 副手槽；
- 玩家可用的 body armor / saddle 映射槽。

光标正在拖动的物品、末影箱、打开的外部容器、潜影盒或收纳袋内部的嵌套物品不属于上述原版玩家物品栏。
空栈、零数量栈和仅有相同名称/组件的其他物品不计入。

主手和副手本身属于玩家物品栏。因此玩家手持永生图腾时，优先应用“不消耗的背包免死”语义；
不会退化为一次性普通图腾。

## 3. 玩家背包免死契约

只要任一有效永生图腾存在于玩家物品栏：

1. `health < 1.0F` 时立即纠正为 `1.0F`。
2. `health` 为 `NaN`、正无穷或负无穷时立即纠正为 `1.0F`。
3. 正常伤害仍可产生盔甲消耗、击退等受伤副作用，但不能把玩家留在 1 点生命以下。
4. `minecraft:generic_kill`（包括原版 `/kill`）在伤害入口即被拒绝，不产生巨量伤害数值。
5. 直接调用 `LivingEntity#kill`、`ServerPlayer#die`、NeoForge 的死亡事件钩子、
   `remove(KILLED)` 或 `setRemoved(KILLED)` 都不能进入死亡或实体移除流程。
6. `LivingDeathEvent` 不应被发布；客户端不应收到死亡包或打开死亡界面；服务端不应产生掉落、
   死亡消息、死亡统计、死亡计时或死亡重生。
7. 每 tick 的前后都进行状态校验，作为对直接 SynchedEntityData 改写、异常事件顺序和其他模组代码的兜底。
8. `dead`、`deathTime` 和 `Pose.DYING` 等可能残留的半死亡状态会在保护生效时清理。
9. 永生图腾不会因为玩家背包免死能力而消耗。

## 4. 离开物品栏后的恢复语义

所有关键拦截点都实时查询物品栏，不持久化“曾经不死”的布尔状态。因此最后一个永生图腾离开物品栏后：

- `isAlive`、`isDeadOrDying`、`setHealth`、`kill`、`die` 和 `remove` 立即回到原版逻辑；
- 后续伤害可以正常致死；
- 不保留无敌属性、能力标记或永久 attachment；
- 保护期间已经被纠正到 1 点的生命值不会被人为改回此前的非法值。

物品移除和伤害均由游戏主线程处理，实时查询避免了缓存失效窗口。

## 5. 非玩家语境

永生图腾自身具有与原版不死图腾相同的死亡保护组件。因此非玩家生物把它放在主手或副手并受到
普通可图腾保护的致命伤害时，原版 `checkTotemDeathProtection` 会消耗一个物品、设为 1 点生命并应用
再生、伤害吸收和抗火效果。

原版标记为绕过死亡保护的伤害仍可绕过这一“普通图腾”行为；玩家背包免死则由独立的玩家保护层负责。
箱子等静态容器中的物品不会主动保护任何实体。

## 6. 双端与兼容性策略

- 服务端是生命值和死亡流程的权威侧。
- 通用 Mixin 同时作用于客户端和服务端的 `LivingEntity` / `Entity` 判定，避免客户端短暂进入死亡 GUI。
- 物品栏和生命值均沿用原版同步；不引入每 tick 自定义网络包。
- 注册一个从不发送内容的非 optional 双向 play payload。它只参与 NeoForge 登录通道协商，
  缺少本模组或网络协议版本不同的一侧会被拒绝，不产生运行期流量。
- 正常伤害优先使用 NeoForge 事件，只对事件不能覆盖的 `kill`、`die`、判死和最终移除入口使用窄 Mixin。
- 仅阻止 `RemovalReason.KILLED`。以下正常生命周期原因必须放行：
  `DISCARDED`、`UNLOADED_TO_CHUNK`、`UNLOADED_WITH_PLAYER`、`CHANGED_DIMENSION`。
- 死亡重生包与 `PlayerList#respawn(..., KILLED)` 有额外兜底，防止原玩家未被移除却又创建重复 UUID 的新玩家。
- Java `final` 只能阻止继承重写，不能阻止 Mixin 在 `Entity#setRemoved` 入口注入检查。

## 7. 明确边界

本实现防御原版及遵循 Minecraft/NeoForge 实体 API 的代码杀路径，包括伤害、`/kill`、`kill()`、
`die()`、死亡事件钩子、`remove(KILLED)` 和死亡重生。

它不会阻止以下操作，因为这些属于服务器生命周期管理或恶意任意代码执行，而不是“死亡”：

- 玩家主动退出、被踢出或连接断开；
- 服务器关闭、世界卸载和维度切换；
- 代码直接篡改 `PlayerList`、实体管理器内部集合或网络连接；
- 另一个核心模组覆盖相同方法并故意忽略所有兼容钩子。

阻止这些操作会制造幽灵玩家、重复 UUID、追踪器泄漏或无法关闭服务器，因此不应被定义为免死能力。

## 8. 验证矩阵

### 必须通过

- [ ] 物品可在创造模式“战斗”标签页取得。
- [ ] 九图腾 3×3 配方正确产出一个永生图腾。
- [ ] 物品模型能清楚区分工件面板与局部能量区域；核心呼吸、眼睛异步闪烁和短促故障足够明显，
  且不存在整图高饱和覆盖或连续滚动扫描线，缺失纹理检查通过。
- [ ] 永生图腾在快捷栏、主背包、护甲槽或副手时，普通致命伤害最终为 1 点生命。
- [ ] 永生图腾位于非选中快捷栏时仍生效。
- [ ] `/kill` 显示命令已执行但玩家不受伤、不死亡。
- [ ] 直接 `kill()`、`die()`、`remove(KILLED)` 与 `setRemoved(KILLED)` 无效。
- [ ] `setHealth(0)`、负数、`NaN`、正负无穷最终都为 `1.0F`。
- [ ] 不产生 `LivingDeathEvent`、死亡消息、掉落、死亡统计、死亡界面或重生实例。
- [ ] 移走最后一个永生图腾后，下一次致命伤害按原版正常死亡。
- [ ] 玩家退出、重连、切换维度和结束诗传送正常。
- [ ] 非玩家生物手持永生图腾时按普通图腾消耗并获得原版效果。
- [ ] 主手按住右键刚好 3 秒后传送回当前出生点，出生点缺失时回退到世界出生点。
- [ ] 引导期间击退抗性为满值、移动速度降低 20%，并持续听到紫水晶叮铃声。
- [ ] 引导开始时立即增加 20 点 Exhaustion，并施加 6 秒虚弱 V。
- [ ] 重复开始引导会刷新虚弱 V 的 6 秒时长，但不会无限制叠加虚弱。
- [ ] 中途打断或正常完成都会施加 18 秒失明 I，重复触发时失明剩余时长累加。
- [ ] 中途松右键、切换主手物品或移除物品后不会传送，属性修饰符恢复正常。
- [ ] 传送前在原位置生成一次 flash 粒子。
- [ ] 成功传送后图腾进入 60 tick 冷却；传送被取消时不进入冷却。
- [ ] 冷却期间无法右键重新引导，但物品仍可持有、掉落并继续提供背包免死等非右键功能。

### 兼容性回归

- [ ] 普通非致命伤害、盔甲、吸收生命、击退和伤害统计不被全局取消。
- [ ] 未携带永生图腾的玩家和所有其他生物完全走原版逻辑。
- [ ] 客户端与专用服务端均能独立启动，Mixin 注入点全部命中。
- [ ] 只有客户端或只有服务端安装时，注册表/模组握手拒绝不兼容连接，而不是产生静默错位。

## 9. 当前自动验证记录（2026-08-23）

- `gradlew clean build` 与最终 `gradlew build` 均成功。
- `runData` 在物理客户端数据环境成功加载全部公共 Mixin 和客户端事件订阅器。
- 专用服务端成功解析 1586 个配方，其中运行期断言确认
  `immortalitytotem:immortality_totem` 确实存在。
- 开发期 FakePlayer / ServerPlayer 自测通过：
  - 原版死亡保护组件存在；
  - `0`、负数、`NaN`、`+Infinity`、`-Infinity` 全部同步纠正为 `1.0F`；
  - 普通 1000 点致命伤害未被整体取消，最终停在 `1.0F`；
  - 直接 `generic_kill`、`kill()`、`ServerPlayer#die()`、`remove(KILLED)`、
    `setRemoved(KILLED)` 和 `respawn(..., KILLED)` 均被阻止；
  - 调用 NeoForge 死亡钩子时返回已取消，监听探针确认 `LivingDeathEvent` 没有被发布；
  - 移走最后一个物品后，`setHealth(0)`、`isDeadOrDying()` 与 `isAlive()` 恢复原版结果；
  - 携带物品的测试玩家仍能以 `CHANGED_DIMENSION` 正常移除。
- 动画遮罩为 16×640 RGBA，共 40 个 16×16 帧；37 个普通帧的 126 个非透明像素与原版
  图腾 alpha mask 完全一致，三个故障帧各只多出一个轮廓外色差像素。`.png.mcmeta` 使用
  `frametime: 2`，完整循环约 4 秒。
- 尚需人工游戏内验收：创造标签位置、最终 GUI 观感、真实玩家联机握手，以及非玩家生物手持消耗动画。
